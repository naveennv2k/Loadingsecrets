name: Say Hello

on:
  workflow_dispatch:

jobs:
  run-my-action:
    name: Checks if this action works
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - id: getSecrets
        uses: ./
      - name: getSecrets
        run: echo "{{steps.getSecrets.outputs.username}}"